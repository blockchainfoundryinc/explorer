.panel.panel-default.hidden-xs
  .panel-heading
    strong #{settings.locale.sys_assetbalances}
  table.table.table-bordered.summary-table
    tbody
      each asset in allocations
        tr
          td
            b #{asset.symbol}&nbsp;
            | (
            a.loading(href='/asset/#{asset.asset_guid}',style='word-break: break-all') #{asset.asset_guid}
              if asset.isOwner
                +ownerIcon
            | )
          td #{asset.allocation_balance} #{asset.symbol}
            if asset.balance > 0
              span(title="Unallocated balance", style="cursor:pointer")  (#{asset.balance})
